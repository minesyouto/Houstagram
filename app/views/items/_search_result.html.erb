<% if @items.present? %>
  <% @items.each do |item| %>
      <p><%= item.name %></p>
      <p><%= item.price %></p>
      <%= link_to image_tag(item.image, size: "200x200"), "/" %>
         
  <% end %>
 <% end %>