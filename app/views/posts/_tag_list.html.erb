<!--@postに格納されているtag_listの情報をtagに格納していき、表示している-->

<% tag_list.each do |tag| %>
    <span class="badge badge-primary mr-1 p-2">
        <%= link_to tag, posts_path(tag_name: tag), class: "text-white" %>
    </span>
<% end %>